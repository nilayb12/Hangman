{
  "name": "hangman",
  "version": "1.0.0",
  "private": true,
  "description": "Hangman game — vanilla JS PWA. This package.json is for development linting only; the game itself ships as plain static files with no build step.",
  "type": "module",
  "scripts": {
    "lint": "eslint scripts/*.js sw.js",
    "lint:html": "html-validate index.html",
    "check": "npm run lint && npm run lint:html"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "html-validate": "^8.24.0"
  }
}
